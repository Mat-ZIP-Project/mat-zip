# ???

# DB
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Naver OCR API
naver.ocr.client-secret=${NAVER_OCR_CLIENT_SECRET}
naver.ocr.api-url=${NAVER_OCR_API_URL}


#security key setting
spring.jwt.secret=${SPRING_JWT_SECRET}

# 환경설정 properties 임포트
spring.config.import=optional:application-secret-prod.properties

iamport.rest-api-secret=${IAMPORT_REST_API_SECRET}
iamport.rest-api-key=${IAMPORT_REST_API_KEY}

# 카카오맵 API
kakao.map.base-url=${KAKAO_MAP_BASE_URL}

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.filter.CorsFilter=DEBUG

